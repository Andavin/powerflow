# Powerflow web app.
#
# Runs as its own container, separate from QuestDB. QuestDB publishes port 9000
# on the host, so this reaches it via host.docker.internal (see extra_hosts) —
# `localhost` inside a container is the container itself, not the host.
# Secrets and timezone/device come from ./.env.
#
#   docker compose up -d --build

services:
  powerflow:
    build: .
    image: powerflow:latest
    restart: unless-stopped
    ports:
      - "3007:3000"
    env_file:
      - .env
    environment:
      POWERFLOW_DATA_MODE: live
      # QuestDB is in another container with port 9000 published on the host;
      # reach it through the host gateway (mapped by extra_hosts below).
      QUESTDB_URL: http://host.docker.internal:9000
      POWERFLOW_AUTH_DISABLED: "0"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test:
        - CMD-SHELL
        - node -e "fetch('http://localhost:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
